const initialState = {
  questions: [],
  currentIndex: 0,
  isPlaying: false,
};
const getters = {
};
const actions = {
};
const mutations = {
};

export default {
  state: { ...initialState },
  getters,
  actions,
  mutations,
};
